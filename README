rainman is a pure python implementation of the bittorrent protocol on top of
tornado.

currently undergoing a refactor, but most of the individual components are
tested and functional.

to run tests:

  >> ./pants tests/rainman:all -v
